#!/bin/bash
# This script shows the system defined variables

echo ${SHELL}
echo ${HOME}
echo ${OSTYPE}
echo ${PATH}
echo ${$} #Process ID
echo ${PPID} #Parent process id
echo ${PWD}
echo ${HOSTNAME}
echo ${UID}
sleep 5
echo ${SECONDS}